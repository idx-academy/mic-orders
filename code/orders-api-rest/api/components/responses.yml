components:
  responses:
    NotFoundResponse:
      description: Not found
      content:
        application/json:
          schema:
            $ref: '../components/schemas.yml#/components/schemas/NotFound'
    BadRequestResponse:
      description: Bad Request
      content:
        application/json:
          schema:
            $ref: '../components/schemas.yml#/components/schemas/BadRequest'
    Unauthorized:
      description: User is unauthorized
      content:
        application/json:
          schema:
            $ref: '../components/schemas.yml#/components/schemas/Unauthorized'